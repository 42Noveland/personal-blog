---
title: 排版测试：这篇用来验收渲染效果
date: 2026-09-10 10:00
tags: [建站, Markdown]
summary: 标题、列表、表格、代码块、引用、任务清单——新增元素时先来这里看一眼。
---

这篇没有阅读价值，只有验收价值。渲染器改完之后，所有元素都应该在这里看起来正常。

## 行内元素

**粗体**、*斜体*、***粗斜体***、`行内代码`、~~删除线~~、==高亮==、以及普通链接 [示例站点](https://example.com)。

一次硬换行：  
这一行应该紧贴着上一行。

## 列表

- 无序项之一
- 无序项之二
  - 嵌套项（缩进两个空格）
  - 另一个嵌套项
- 无序项之三

1. 有序第一步
2. 有序第二步
3. 有序第三步

任务清单：

- [x] 渲染段落与标题
- [x] 渲染代码块并加复制按钮
- [ ] 检查移动端表现

## 引用与分隔线

> 引用块应该有一条左侧色带，并且保持可读的行长。
>
> 引用里也可以有 **强调** 和 `代码`。

---

## 代码块

```js
// 前端：切换主题并持久化
function applyMode(mode) {
  document.documentElement.dataset.mode = mode;
  localStorage.setItem('blog-mode', mode);
  return mode === 'dark' ? 'light' : 'dark';
}
```

```bash
# 部署时常用的几条命令
ss -tlnp | grep :3081
curl -sI https://127.0.0.1:2270/blog/ -k | head -1
kill -HUP $(cat /root/blog/blog.pid)
```

## 表格

| 项目 | 值 | 说明 |
|:--|--:|:--|
| 文章数 | 6 | 含本篇 |
| 每页条数 | 8 | config.json 可改 |
| 阅读时长 | 按中英混排估算 | 中文 400 字/分 |

## 图片

![示例图片](https://picsum.photos/seed/blog/900/360)

如果上面这张图能显示，说明外链图片渲染正常。
